<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aptitudeapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AptitudeApp">
        <activity android:name=".HomeActivity"></activity>
        <activity android:name=".Logical_Activity" />
        <activity android:name=".Verbal_Activity" />
        <activity android:name=".SignOutActivity"/>
        <activity
            android:name=".General_Activity"
            android:label="@string/title_activity_quiz1"
            android:theme="@style/Theme.AptitudeApp.NoActionBar" />
<!--        <activity android:name=".HomeActivity" />-->
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>